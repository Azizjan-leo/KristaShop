@model List<Module.Client.Business.Models.NewUserDTO>

@if (Model.Any()) {
    <h1 class="dashboard-title">Новые регистрации</h1>

    <div class="dashboard-header violet-g">
        <h3>
            <svg class="krista-icon krista-user krista-icon-size16">
                <use xlink:href="#krista-user"></use>
            </svg> Новые регистрации
        </h3>
        <h2>новых клиентов: @Model.Count</h2>
    </div>

    <div class="col-12 table-responsive">
        <table class="table table-striped table-bordered text-break dataTable newRegistrationsTable" style="width: 100%; background-color: #ffffff;">
            <thead>
            <tr>
                <th>Клиент</th>
                <th>Контакты</th>
                <th>Город и название ТЦ</th>
                <th>Менеджер</th>
                <th>Дата</th>
                <th>Действия</th>
            </tr>
            </thead>
        </table>
    </div>
    
    {
        <script>
            const newRegistrationsData = @Html.AsJsObject(Model); 
        </script>        
    }
}